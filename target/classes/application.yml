server:
  port: 8081
# DataSource Config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/vuemanager?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    # username和password指向数据库的用户名和密码
  security:
    user:
      name: root
      password: 123456
  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 3000
mybatis-plus:
  mapper-locations: classpath*:/mapper/**Mapper.xml
vuemanager:
  jwt:
    header: Authorization
    expire: 7200
    secret: 'fi8n3439n439n431d9ne9343fdfet49h'
